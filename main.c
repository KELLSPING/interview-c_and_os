#include <stdio.h>
#include <limits.h>

int main(void)
{
    #include <limits.h>

printf("The minimum value of INT = %x\n", INT_MIN);
printf("The maximum value of INT = %x\n", INT_MAX);

    return 0;
}